# Mobile experiments, Flutter version

This branch holds Flutter rebuilds of the experiments on the main branch. Each folder is a standalone Flutter app with its own README.

Every project runs the same way:

    flutter pub get
    flutter run

Built with Flutter 3.44.
