# Quire

A reader for PDF, Word, spreadsheet and Markdown files, with pages you peel and search that sweeps a highlighter across every match.

## Run

    flutter pub get
    flutter run

## Tests

    flutter test

The images under `test/goldens` were rendered on Windows. Regenerate them with
`flutter test --update-goldens` before comparing on another platform.
