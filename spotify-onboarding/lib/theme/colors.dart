import 'dart:ui';

/// The palette the onboarding flow is built from.
abstract final class AppColors {
  static const ink = Color(0xFF0D0D0D);
  static const muted = Color(0xFF8E8E93);
  static const white = Color(0xFFFFFFFF);
  static const spotify = Color(0xFF1ED760);
  static const locationAccent = Color(0xFF4DA3FF);
  static const locationDisc = Color(0xFF0A84FF);

  /// Background and label of the Skip pill in the header.
  static const pill = Color(0xFFF1F1F1);
  static const pillLabel = Color(0xFF6F6F73);
}
